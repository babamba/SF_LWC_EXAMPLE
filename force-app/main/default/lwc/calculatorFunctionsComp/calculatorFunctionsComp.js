const fnum=20;
const snum=30;
const add2numbers = (fnumber, snumber) => parseInt(fnumber)+parseInt(snumber);
const sub2numbers = (fnumber, snumber) => parseInt(fnumber)-parseInt(snumber);

export {
    add2numbers,
    sub2numbers,
    fnum,
    snum
};